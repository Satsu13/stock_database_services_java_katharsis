package resources.model_data.version;

import defaults.DefaultTable;
import io.katharsis.resource.annotations.JsonApiResource;

@JsonApiResource(type = "versions")
public class Version extends DefaultTable {
}
